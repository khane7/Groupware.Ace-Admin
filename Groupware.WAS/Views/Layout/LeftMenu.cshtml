@{
	ViewBag.PageTitle = "LeftMenu";
}

<script type="text/javascript">
	try { ace.settings.check('sidebar', 'fixed') } catch (e) { }

	$('.nav nav-list').ready(function () {
		//alert("ck");
	});
</script>

<div class="sidebar-shortcuts" id="sidebar-shortcuts">
	<div class="sidebar-shortcuts-large" id="sidebar-shortcuts-large">
		<button class="btn btn-success">
			<i class="ace-icon fa fa-signal"></i>
		</button>

		<button class="btn btn-info">
			<i class="ace-icon fa fa-pencil"></i>
		</button>

		<!-- #section:basics/sidebar.layout.shortcuts -->
		<button class="btn btn-warning">
			<i class="ace-icon fa fa-users"></i>
		</button>

		<button class="btn btn-danger">
			<i class="ace-icon fa fa-cogs"></i>
		</button>

		<!-- /section:basics/sidebar.layout.shortcuts -->
	</div>

	<div class="sidebar-shortcuts-mini" id="sidebar-shortcuts-mini">
		<span class="btn btn-success"></span>

		<span class="btn btn-info"></span>

		<span class="btn btn-warning"></span>

		<span class="btn btn-danger"></span>
	</div>
</div><!-- /.sidebar-shortcuts -->





<ul class="nav nav-list">

	@{
		var status = 0;
		var prev_level = 0;
		var curr_level = 0;
        
		foreach (var item in ViewBag.listTree)
		{
			curr_level = item.tree_level;

			if (prev_level != 0 && prev_level < curr_level)
			{
				@Html.Raw("<ul class='submenu'>");
			}
			else if (prev_level != 0 && prev_level > curr_level)
			{
				@Html.Raw("	</ul>");
				@Html.Raw("</li>");
			}

			@Html.Raw("<li class='' id='" + @item.tree_value + "'>");
			if (@item.tree_value == "#")
			{
				<a href="#" class="dropdown-toggle">
					<i class="@item.html_icon"></i>
					<span class="menu-text"> @item.tree_key </span>
					<b class="arrow fa fa-angle-down"></b>
				</a>
			}
			else
			{
				<a href="javascript:goPage('@item.tree_value');">
					<i class="@item.html_icon"></i>
					@item.tree_key
					<b class="arrow"></b>
				</a>
			}
	 
			prev_level = curr_level;
			status++;
		}
		@Html.Raw("	</ul>");
		@Html.Raw("</li>");
	}

</ul>

<!-- /.nav-list -->



<!-- #section:basics/sidebar.layout.minimize -->
<div class="sidebar-toggle sidebar-collapse" id="sidebar-collapse">
	<i class="ace-icon fa fa-angle-double-left" data-icon1="ace-icon fa fa-angle-double-left" data-icon2="ace-icon fa fa-angle-double-right"></i>
</div>

<!-- /section:basics/sidebar.layout.minimize -->
<script type="text/javascript">
	try { ace.settings.check('sidebar', 'collapsed') } catch (e) { }
</script>


			

